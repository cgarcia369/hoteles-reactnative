import React  from 'react';
import {Container, Text} from 'native-base';

const HotelEditScreen = () => {
	return (
		<Container>
			<Text>
				Country index screen
			</Text>
		</Container>
	);
};

export default HotelEditScreen;
