import React from 'react';
import {Container, Text} from 'native-base';

const LoginScreen= () => {
  return (
    <Container>
      <Text>
        Login screen
      </Text>
    </Container>
  );
};

export default LoginScreen;
